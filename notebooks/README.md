# Notebooks

Exploration and ad-hoc analysis. Notebooks are intentionally separate from
`src/` — they're messy by design. If a notebook produces something reusable,
graduate the logic into `src/voc_analyzer/`.
